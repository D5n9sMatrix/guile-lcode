module servers.scripts;


/*
The effect of R π is to rotate the vector x =
counterclockwise through π2 to produce the vector R π (x) shown
Figure 2.2.5 in Figure 2.2.5. Since triangles 0px and 0qR π (x) are identical,
In other words, R π
*/
void vector(x, y)(ref string)
{
    interface x
    {
        string scripts; // lcode scripts
    }
    interface y
    {
        string lcode;   // lcode textarea
    }
return string;
}
        
