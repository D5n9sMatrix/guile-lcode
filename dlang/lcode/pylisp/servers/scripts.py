#!/usr/bin/python3

class servers:
    def continues(self, lcode=True, play=True, program=True, features=True):
        self.__doc__ = lcode
        self.__doc__ = play
        self.__doc__ = program
        self.__doc__ = features

        if lcode.denominator is True:
            if play.numerator is True:
                print(program)
            else:
                return program
                pass
        else:
            return exit(program)
            pass
                        
